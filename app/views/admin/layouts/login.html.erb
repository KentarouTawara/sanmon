<!DOCTYPE html>
<html data-theme="emerald">
  <head>
    <title>
      <%= page_title(yield(:title)) %>
    </title>

    <script src="https://kit.fontawesome.com/3a0bc94062.js" crossorigin="anonymous"></script>

    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="max-w-xl mx-auto">
      <%= render  'shared/flash_message' %>
      <%= form_with url: admin_login_path, method: :post, class: "bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4" do |f| %>
        <h1 class="text-center text-2xl mb-6 text-gray-600 font-bold font-sans">
          管理画面ログイン
        </h1>
        <div class="mb-4">
          <%= f.label :penname, User.human_attribute_name(:penname), class: "label" %>
          <%= f.text_field :penname, class: "input input-bordered shadow w-full focus:outline-none focus:shadow-outline" %>
        </div>
        <div class="mb-4">
          <%= f.label :password, User.human_attribute_name(:password), class: "label" %>
          <%= f.password_field :password, class: "input input-bordered shadow w-full focus:outline-none focus:shadow-outline" %>
        </div>
        <div class="flex justify-center">
          <%= f.submit "Login", class: "btn btn-primary" %>
        </div>
      <% end %>
    </div>
  </body>
</html>